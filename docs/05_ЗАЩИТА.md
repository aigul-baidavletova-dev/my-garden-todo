# 05. Подготовка к защите

Ответы здесь **не даны**: сформулируй их своими словами, лучше вслух.
Если ответ не получается, рядом указано, где перечитать.

## Базовые вопросы

1. Что такое чистая функция? Покажи в своём коде одну чистую и одну нечистую. *(теория §1)*
2. Почему `Date.now()` нельзя вызывать внутри редьюсера? Как ты решила эту проблему? *(архитектура §2)*
3. Что такое иммутабельность? Покажи, как у тебя переключается `done` без мутации. *(теория §2)*
4. Чем `arr.sort()` опасен в ФП-коде? Чем его заменить? *(теория §2)*
5. Что такое функция высшего порядка? Назови 3 места в проекте, где они используются. *(теория §4)*
6. Чем `map`, `filter` и `reduce` отличаются друг от друга? *(теория §4)*
7. Как в приложении реализована фильтрация? Почему отфильтрованный список не хранится в состоянии? *(архитектура §1)*

## Продвинутые вопросы

8. Что такое замыкание? Где оно есть в `filters.js` или `fp.js`?
9. Как работает твой `pipe`? Распиши вызов `pipe(f, g)(x)` по шагам `reduce`.
10. Почему spread (`{...obj}`) не защищает вложенные массивы? Как это влияет на проект?
11. Зачем нужен `deepFreeze`, если код и так не мутирует данные?
12. Почему редьюсер для неизвестного действия возвращает **тот же** объект, а не копию?
13. Как работает «Вернуть» после удаления? Почему там нет копирования?
14. Почему сад всегда показывает все задачи, а список — только отфильтрованные?
15. Где в приложении побочные эффекты и как они изолированы?
16. Как ты защитилась от XSS при выводе названий задач?

## Вопросы с подвохом

17. `const arr = []; arr.push(1);`: это нарушение иммутабельности? Ведь `const`!
18. Функция внутри себя создаёт массив и делает `push` в него, а потом возвращает. Она чистая?
19. Является ли `render` чистой функцией? А может ли быть?
20. Зачем в `main.js` есть `let`, если мы пишем в ФП-стиле?

## Вопросы по 3D-саду

21. Почему нельзя рисовать деревья через `Math.random()`? Как тогда у каждой задачи своё дерево? *(архитектура §7.1)*
22. Что такое «зерно» и почему `plantFor` — чистая функция, хотя деревья выглядят случайными?
23. Где здесь используется `scan` и чем он отличается от `reduce`?
24. Как устроена спираль «золотого угла» и почему деревья не налезают друг на друга? *(§7.2)*
25. Почему `phaseOfHour` принимает час аргументом, а не вызывает `new Date()` сама? *(§7.4)*
26. Как протестировать «ночь», если тесты запускаются днём?
27. three.js изменяет объекты сцены. Почему это не нарушает ФП в проекте? Где проходит граница?
28. Что делает `sync(tasks)`: как сцена понимает, какое растение добавить, вырастить или убрать?
29. Зачем `scene.js` загружается через `import()`, а не обычным `import` в начале файла?
30. Задание — «только HTML, CSS и JS». Как ты обоснуешь использование three.js?

## Вопросы по отдельным файлам

31. `task.js`: почему `id` и `createdAt` приходят в `createTask` аргументами, а не создаются внутри?
32. `tasks.js`: почему `updateTaskById` оставляет незатронутые задачи **теми же** объектами?
33. `fp.js`: `deepFreeze` меняет объект (замораживает его). Можно ли назвать её чистой функцией?
34. `events.js`: почему `dispatch` приходит в `bindEvents` аргументом, а не импортируется из `main.js`?
35. `index.html`: зачем `<form>`, если можно просто кнопку? Что даёт `<label>`?

## Демонстрация (порядок показа)

1. Посадить 4 задачи → отметить две: в саду выросли деревья разных пород. Покрутить сад мышью.
2. F5: всё сохранилось, **деревья те же самые**. Объяснить: форма — чистая функция от id.
3. Переключить «Утро → Вечер → Ночь»: небо, свет, светлячки, цвета панели. Вернуть «Авто».
4. Отфильтровать «Выросли», удалить задачу → «Вернуть».
5. Свернуть панель («Смотреть сад») и показать сад целиком.
6. Открыть `tests/index.html`: всё зелёное. Показать тест «23 часа → ночь».
7. Показать код: `plant.js` (зерно и псевдослучайность), `daytime.js` (чистое время),
   `tasks.js` (ФВП), `reducer.js` (чистота), `scene.js` (где начинается изменяемый мир).
